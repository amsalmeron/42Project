#include "libft.h"


void extra_tests()
{
	// ft_substr
/*
	char hello_substr[] = "hello world";
	char *sub = ft_substr(hello_substr, 3, 5);
	char substr[] = "lo wo";
	int test_substr = 0;
	int i = 0;
	while (sub[i])
	{
		if (sub[i] != substr[i])
		{
			test_substr = 0;
			break;
		}
		i++;
	}
	if (test_substr)
		printf("\x1b[32mPASSED\x1b[0m\n");
	else
		printf("\x1b[32mFAILED\x1b[0m\n");
*/
	// ft_strjoin
	// DRAFT CREATED

	// ft_strtrim
	// DRAFT STARTED

	// ft_strsplit

	// ft_itoa
	// DRAFT STARTED

	// ft_strmapi

	// ft_striteri

	// ft_putchar_fd

	// ft_putstr_fd

	// ft_putendl_fd

	// ft_putnbr_fc

}
